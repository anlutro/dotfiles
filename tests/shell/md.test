#!/bin/bash
set -euo pipefail
source $TESTS_DIR/common.sh

for f in $CONFIG_DIR/shell/shrc.d/*.sh; do source $f; done
tmpdir=$(mktemp -d)
cd $tmpdir

md -v foo
assert_equals $tmpdir/foo $PWD

rm -rf $tmpdir
