#!/bin/sh

if [ "$1" = '-h' ]; then
	echo 'genpw <len> [+]'
	exit
fi

len=${1:-8}

chars="abcdefghjkmnpqrtuvwxyz2346789"
if [ "$2" = "+" ]; then
	chars=$chars"ABCDEFGHJKMNPQRTUVWXYZ!@#$%&.:,;-"
fi

cat /dev/urandom | tr -dc $chars | head -c${1:-$len}
echo # newline
